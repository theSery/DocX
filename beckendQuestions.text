1. PUT /api/personal-data
    * Please make it possible to send notificationAddress as an empty string ("") to clear the current value, or provide another supported way to disable/remove notificationAddress.
2. POST /api/complaints/{id}/send

{
  "recipientType": "email",
  "recipientEmail": "string",
  "addresseeEmail": "string",
  "attachedDocuments": [
    1,
    2,
    3
  ]
}

I have a few questions about this endpoint:
lawyer, addressee, email"   (recipientType, voncenq voroshum), voncenq haskanum vor depqum uxarkenq es
* What values are supported for recipientType? Is there a list of allowed values (enum)?
* What IDs should be passed in attachedDocuments? Which endpoint returns these document IDs?
* Could you provide an example of a valid request for this endpoint?

3. Favorite APIs
    * We need Favorite APIs for Complaints, not for Templates (FavoriteTemplates).
4. Privacy Policy and Terms of Use APIs
    * /api/privacy-policy returns an error. Please check and fix this API.
    * We also need an API for Terms of Use content.